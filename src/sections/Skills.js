import React from "react";
import Section from "../components/Section";
import { links } from "../constants";

const Skills = () => {
  return (
    <Section id={links.skills}>
      <div>Skills</div>
    </Section>
  );
};

export default Skills;
